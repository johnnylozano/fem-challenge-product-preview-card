import { ShoppingIcon } from '../ShoppingIcon';
import { Btn } from './styles';

export const Button = () => {
   return (
      <Btn>
         <ShoppingIcon />
         Add to Cart
      </Btn>
   );
};
